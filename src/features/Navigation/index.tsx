import { Nav, Logo, LogoIcon, LogoTitle } from "./styled";

export const Navigation = () => {
  return (
    <Nav>
      <Logo>
        <LogoIcon />
        <LogoTitle>DashBoard</LogoTitle>
      </Logo>
    </Nav>
  );
};
